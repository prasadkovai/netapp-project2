import React from 'react';


export default props => (
    <div className="grid">
        
        <div className="main-content">
            
                {props.children}
           
        </div>
    </div>
);
